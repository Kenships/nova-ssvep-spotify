See `hardware/eego_bringup_notes.md` and `hardware/eego_lsl_launch.md` for
the actual ant-neuro eego investigation notes and bring-up checklist. This
file is a pointer so both `docs/` and `hardware/` don't drift out of sync
with duplicated content.
